import React from "react";
import preloader from "../../../assets/preloaders/preloader1.gif"


let Preloader = (props) => {
    return <div>
       <img src={preloader}></img>
    </div>
}

export default Preloader;